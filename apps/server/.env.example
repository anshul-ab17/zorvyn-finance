# Database
DATABASE_URL="postgresql://pockit:pockit_secret@localhost:5432/pockit_db"

# Redis
REDIS_URL="redis://localhost:6379"

# Auth
JWT_SECRET="change_this_to_a_strong_random_secret"

# Server
PORT=4000
FRONTEND_URL="http://localhost:3000"

# SMTP is configured per-user via the Settings page (stored in the User table).
# No global SMTP env vars are needed.
